<div class="container center_div">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">

      <h3> Add a review </h3>

      <%= form_for :review, url: reviews_path, :html => { class: "form-horizontal" } do |f| %>
        <div class="form-group">
          <fieldset>
            <p>
              <strong>Select school</strong><br>
              <%= f.collection_select :school_id, @schools, :id, :name, {class: "form-control", style:"200px"}%>  </p>
            </p>

            <p>
              <strong>Is the school located in a safe neighbourhood? </strong><br>
              <%= f.select :surroundings_good, options_for_select(%w[Yes No]), {class: "form-control"} %>
            </p>

            <p>
              <strong>Do you believe that the school has verified the background of the teachers?</strong><br>
              <%= f.select :teacher_bg, options_for_select(%w[Yes Maybe No]), class: "form-control" %>
            </p>

            <p>
              <strong>Do you believe that the school has verified the background of the support staff including school bus drivers?</strong><br>
              <%= f.select :helpers_bg, options_for_select(%w[Yes Maybe No]), class: "form-control" %>
            </p>

            <p>
              <strong>Are you happy with the security arrangements at the school?</strong> <br>
              <%= f.select :security, options_for_select(%w[Yes Maybe No]), class: "form-control"%>
            </p>

            <p>
              <strong>Do you / does your child feel safe at school? </strong><br>
              <%= f.select :safe_at_school, options_for_select(%w[Yes No]), class: "form-control"%>
            </p>

            <p>
              <strong>Comments <br>
              <%= f.text_area :comments, {:cols => "55", :rows => "7", class: "form-control", placeholder: "Leave a comment..."} %>
            </p>

            <p>
              <strong>Overall rating</strong><br>
              <%= f.select :overall_rating, options_for_select(1..5), class: "form-control " %>
            </p>

            <div class="control-group">
              <div class="controls">
              <%= f.submit "Submit review", { class: "btn btn-primary" } %>
                </div>
            </div>
          </fieldset>
        </div>
      <% end %>


      </div>
    </div>
  </div>